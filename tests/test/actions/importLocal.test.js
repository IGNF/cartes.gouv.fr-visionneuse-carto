import { describe, it, expect, vi, beforeEach } from 'vitest'
import * as importLocal from '../../../src/actions/importLocal/importLocal.js'

describe('import ', () => {
  it('1 + 1 =2', () => {
    expect(1 +1).toEqual(2)
  })
})